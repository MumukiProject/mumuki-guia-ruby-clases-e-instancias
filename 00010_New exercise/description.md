Como viste recién, la `salud` no se comparte entre `bouba` y `kiki` a pesar de que ambos sean instancias de `Zombie`.

Pero nos quedó un método misterioso por aclarar: `initialize`. Al trabajar con clases